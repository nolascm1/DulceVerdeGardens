<% @plants.each do |plant| %>
 <div class="booyah-box col-10 offset-1">
    <i><%= plant.zipcode %></i><br /><br />
     <% if plant.user.present? %>
      <small><%= plant.user.email %></small>
    <% end %>
    </div>
<% end %>
